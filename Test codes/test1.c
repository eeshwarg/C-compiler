#include<stdio.h>
#define A hakuna matata
/* This is a
   multi line comment
*/

int main()
{
	//a"hello";
	int i,j,n,fact=1;
	// printf("Enter an integer:\n");
	// scanf("%d",&n);

	while(n>0)//Until n becomes zero
	{
		fact = 10 - n;
		if (fact < n)
		{
			n = i + j;
			i = i * 1;
			break;
		}
		else
			fact = 5;
		n--;
		fact = ++n * i++;
	}

	char a,b,c,d='a';
	if(b<'t')
		a = 'a';

	c = b + d * a;
	b = 't';
	n++;
	char e;

	do{
		a = b + c - d * e;
		break;
		if(a > b)
			c = d * e;
	}while(n>0);

	int arr[5];
	j = arr[i] + fact * i;
	// printf("Factorial = %d\n",fact);

	return 0;
}
