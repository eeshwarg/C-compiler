#include<stdio.h>
#define A hakuna matata
/* This is a
   multi line comment
*/

int main()
{
	//a"hello";
	int n,fact=1;
	// printf("Enter an integer:\n");
	// scanf("%d",&n);

	while(n>0)//Until n becomes zero
	{
		fact = 10 - n;
		if (fact < n)
			break;
		else
			continue;
		n--;
	}

	char a,b,c,d='a';
	if(b<'t')
		a = 'a';

	c = b + d * a;
	b = 't';
	n++;
	int e;

	// printf("Factorial = %d\n",fact);

	return 0;
}
